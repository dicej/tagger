fix state query
  duplicates for images with multiple tags
  get all tags for each image when filtering by tag
write tests
  ensure all endpoints require auth except /token
  test all endpoints and both patch types
  test complicated tag expressions
  test full size, large, and small image downloads
work on front end
video support
