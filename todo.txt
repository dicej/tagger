add "not" support to tagexpression
  and to UI
  server tests
HEAD and range requests?
remember and skip known bad files when syncing
performance
  O(n) ops in FE
  stream files instead of loading them into memory in BE
  TagExpressionParser lazy_static?
login/logout
  disallow selection when logged out
clean up
  remove javascript:void() usage
  modularize FE and BE
  componentize FE
  reduce inline template code in FE
  remove State from FE?
minimize expensive clones
break up tests
video tests
document
demo site?
  e.g. sandbox mode where database is copied to in memory db that is dropped on token expiration (e.g. 30 minutes)
  run it in a docker container for isolation
announce
